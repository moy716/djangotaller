###Lista de comandos 
git tutorial config
git init 
git status 
git add "name"
git commit -m
git log 
git add --all
git checkout "commit"
git checkout master

git config --global user.name "Moises"
git config --global user.email moy716@hotmail.c